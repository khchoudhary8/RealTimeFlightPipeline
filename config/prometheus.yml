global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: 'flight-producer'
    static_configs:
      - targets: ['ingestion-service:8000']
    scrape_interval: 15s
    metrics_path: /

  # Optional: Add Dagster metrics if they expose any
  # - job_name: 'dagster'
  #   static_configs:
  #     - targets: ['localhost:3001']
  #   scrape_interval: 30s
